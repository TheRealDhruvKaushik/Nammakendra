Please make the "Upload Document" button on my existing website functional. The current button is a dummy but needs to be connected to the following functionality:

The upload button should allow users to:

📁 Browse files (PDF or image)

📷 Take a photo using the camera

🖼️ Select from gallery (images)

Once a document (PDF or image) is uploaded, do the following:

Extract text from the document:

PDFs: Use PyMuPDF (fitz) to extract text.

Images: Use Tesseract OCR with Kannada and English language support.

Detect the language of the extracted text (Kannada vs. English) using langdetect or a script-based method.

If the language does not match the user's selected mode ("eng" or "kan"):

Automatically translate the document text using IndicTrans2 via Hugging Face.

If the text matches the user mode (i.e., Kannada for Kan mode or English for Eng mode):

Skip translation.

The final document should be sent to LLaMA 3 (via Groq API) for simplification.

Simplification:

In Kannada mode, the document should be simplified in plain Kannada.

In English mode, it should be simplified in plain English.

Use custom prompts to simplify the legal text:

Kannada:

yaml
Copy
Edit
ಇದೊಂದು ಕಾನೂನು ಡಾಕ್ಯುಮೆಂಟ್ ಆಗಿದೆ. ಇದನ್ನು ಸರಳ ಕನ್ನಡದಲ್ಲಿ, ಸಾಮಾನ್ಯ ವ್ಯಕ್ತಿಗೆ ಅರ್ಥವಾಗುವ ರೀತಿಯಲ್ಲಿ ವಿವರಿಸಿ. ಇದನ್ನು ಸೂಕ್ಷ್ಮವಾಗಿ ವ್ಯಾಖ್ಯಾನಿಸಿ ಆದರೆ ಕಾನೂನು ಅರ್ಥವನ್ನು ಕಳೆದುಕೊಳ್ಳದಂತೆ ನೋಡಿ.
---
{text}
English:

yaml
Copy
Edit
This is a legal document. Please simplify it in plain English so that a common person can understand it clearly. Retain the legal meaning but remove complex terminology and structure.
---
{text}
After simplification, return the simplified text to the user in their selected language (Kannada or English).

Ensure the following:

Proper error handling for unreadable PDFs, OCR failures, and translation issues.

The frontend should display an upload progress indicator while processing the document.

The document upload process should be seamless and provide feedback to the user (e.g., “Document successfully uploaded”, “Processing in progress”, “Simplification complete”).

UI Details:

The upload button should show “Upload Document” and support the following file upload options:

📁 Browse Files (PDFs, images)

📷 Take a Photo

🖼️ Choose from Gallery

Once the document is uploaded, it should automatically start processing and then display the simplified document (after simplification).

Please ensure that this feature integrates smoothly with the existing backend pipeline and uses the Groq + LLaMA 3 setup for simplification.

